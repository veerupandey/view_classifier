[project]
name = "view-classifier"
version = "0.1.0"
description = "Single-image vehicle view (pose + azimuth) classifier"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }

[tool.setuptools.packages.find]
include = ["view_classifier*"]
